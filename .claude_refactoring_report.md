# VoiceThing Refactoring Report

Generated from deep analysis by 10 parallel agents examining duplicate code, redundant patterns, and consolidation opportunities.

## Executive Summary

**voice_thing.py** is ~8,700 lines with 28 classes and ~150 module-level functions. The analysis found:
- **~500+ lines** of duplicate code in style files
- **~275 lines** of duplicate widget creation patterns
- **~135 lines** of duplicate dialog patterns
- **~107 lines** of duplicate CSS/styling
- **~70 lines** of dead/unused code
- Multiple magic numbers and hardcoded values

---

## 1. Dead Code to Remove (Immediate)

### Unused Imports
```python
# Line 4 - REMOVE
import collections

# Line 6 - REMOVE
import functools

# Line 31 - REMOVE these from import
QPropertyAnimation, QEasingCurve, pyqtProperty

# Line 32 - REMOVE from import
QPainterPath
```

### Unused Functions (~70 lines)
| Lines | Function | Action |
|-------|----------|--------|
| 1171-1174 | `clear_chime_log()` | DELETE - never called |
| 1713-1744 | `_get_tmux_pane_state()` | DELETE - superseded |
| 1747-1766 | `_get_tmux_cursor()` | DELETE - inlined elsewhere |
| 1769-1782 | `_get_tmux_scrollback()` | DELETE - inlined elsewhere |

---

## 2. High-Priority Helper Functions to Add

### 2.1 Widget Creation Helpers (saves ~275 lines)

```python
def make_checkbox(text, checked, tooltip=None, on_change=None, css_size=11):
    """Create a styled checkbox with optional tooltip and handler."""
    cb = QCheckBox(text)
    cb.setChecked(checked)
    cb.setStyleSheet(get_checkbox_css(css_size))
    if tooltip:
        cb.setToolTip(tooltip)
    if on_change:
        cb.stateChanged.connect(on_change)
    return cb

def make_icon_label(icon_name, size=14, color=None):
    """Create a QLabel with an icon pixmap."""
    label = QLabel()
    icon = load_icon(icon_name, color=color or ICON_COLOR_DARK)
    if icon:
        label.setPixmap(icon.pixmap(size, size))
    return label

def make_icon_btn(text, icon_name, on_click=None, icon_size=16, tooltip=None):
    """Create a styled button with icon."""
    btn = QPushButton(text)
    btn.setIcon(load_icon(icon_name, color=ICON_COLOR_DARK))
    btn.setIconSize(QSize(icon_size, icon_size))
    btn.setStyleSheet(get_btn_css())
    if on_click:
        btn.clicked.connect(on_click)
    if tooltip:
        btn.setToolTip(tooltip)
    return btn

def make_pref_label(text, tooltip=None):
    """Create a styled preference label."""
    label = QLabel(text)
    label.setStyleSheet(get_pref_label_css())
    if tooltip:
        set_tooltip(label, tooltip)
    return label

def make_hbox(spacing=8):
    """Create horizontal layout with standard spacing."""
    layout = QHBoxLayout()
    layout.setSpacing(spacing)
    return layout

def make_vbox(spacing=8):
    """Create vertical layout with standard spacing."""
    layout = QVBoxLayout()
    layout.setSpacing(spacing)
    return layout

def make_separator(vertical=True):
    """Create a separator line."""
    sep = QLabel()
    if vertical:
        sep.setFixedWidth(1)
    else:
        sep.setFixedHeight(1)
    sep.setStyleSheet(f"background: {BORDER_COLOR};")
    return sep
```

### 2.2 Dialog Helpers (saves ~135 lines)

```python
def make_button_row(dialog, save_text="Save", cancel_text="Cancel",
                    revert_callback=None, revert_text="Revert"):
    """Create standard save/cancel button row for dialogs."""
    btn_row = QHBoxLayout()
    btn_row.setSpacing(8)
    if revert_callback:
        revert_btn = QPushButton(revert_text)
        revert_btn.setStyleSheet(get_btn_css())
        revert_btn.clicked.connect(revert_callback)
        btn_row.addWidget(revert_btn)
    btn_row.addStretch()
    cancel_btn = QPushButton(cancel_text)
    cancel_btn.setStyleSheet(get_btn_css())
    cancel_btn.clicked.connect(dialog.reject)
    btn_row.addWidget(cancel_btn)
    save_btn = QPushButton(save_text)
    save_btn.setStyleSheet(get_btn_css())
    save_btn.clicked.connect(dialog.accept)
    btn_row.addWidget(save_btn)
    return btn_row

def make_traffic_light_close(on_click):
    """Create red traffic light close button."""
    btn = TrafficLightButton("rgb(255, 95, 87)", "rgb(255, 120, 110)", "macos-close")
    btn.setToolTip("Close")
    btn.clicked.connect(on_click)
    return btn
```

### 2.3 Add to DraggableDialog Base Class

```python
class DraggableDialog(QDialog, DraggableResizableMixin):
    # Add default escape handling
    def keyPressEvent(self, e):
        if e.key() == Qt.Key.Key_Escape:
            self.reject()
        else:
            super().keyPressEvent(e)

    def setup_layout(self, margins=15, spacing=10):
        """Create standard dialog layout."""
        layout = QVBoxLayout(self)
        layout.setContentsMargins(margins, margins, margins, margins)
        layout.setSpacing(spacing)
        return layout
```

---

## 3. Style System Consolidation (saves ~500 lines in styles/)

### 3.1 Add to BaseStyle

```python
class BaseStyle:
    # Add these methods to consolidate duplicate patterns across 11 style files

    def _draw_vignette(self, painter, rect, width, height, radius=12,
                       color=QColor(0, 0, 0), alpha_mult=1.0):
        """Draw edge vignette effect. Used by 6+ styles."""
        # Consolidate duplicate code from mahogany, cyberpunk, supervillain, etc.
        pass

    def _draw_engraved_shadows(self, painter, rect, w, h):
        """Draw engraved shadow effect for panels. Used by 7+ styles."""
        pass

    def _draw_panel_grid(self, painter, w, h, color, h_lines=4, v_lines=8):
        """Draw grid lines on panel. Used by 5+ styles."""
        pass

    def _draw_edge_glow(self, painter, rect, color, direction='top'):
        """Draw edge glow effect."""
        pass

    def _generate_brushed_metal(self, width, height, brightness_range=(145, 195)):
        """Generate brushed metal texture. Used by 3+ styles."""
        pass
```

### 3.2 CSS Builder Methods for BaseStyle

```python
def icon_btn_css(self):
    """CSS for icon-only buttons (no padding)."""
    return self.button_css().replace(
        "padding: 3px 8px;", "padding: 0px; margin: 0px;"
    ).replace("text-align: left;", "text-align: center;")

def small_btn_css(self):
    """CSS for small compact buttons."""
    return self.button_css().replace(
        "padding: 3px 8px;", "padding: 1px 4px; font-size: 10px;"
    )

def tooltip_css(self):
    """Standard tooltip styling."""
    return "QToolTip { background: #333; color: white; border: 1px solid #555; border-radius: 4px; }"

def table_css(self):
    """Standard table widget styling."""
    # Consolidate from TmuxSelectionDialog and ChimeEditorDialog
    pass
```

---

## 4. Audio/Synth Consolidation

### 4.1 Centralize Pitch Shift Calculation

```python
# Replace 4 occurrences of `-12 + S.CHIME_PITCH` with:
def get_effective_shift():
    """Get effective pitch shift (-12 base + user setting)."""
    return -12 + S.CHIME_PITCH
```

### 4.2 Centralize Chime Audio Parameters

```python
def get_chime_audio_params():
    """Get current chime audio parameters as dict."""
    return {
        'shift': get_effective_shift(),
        'volume': S.CHIME_VOLUME,
        'program': S.CHIME_PROGRAM,
    }
```

### 4.3 Unified Chime Pattern Lookup

```python
def get_chime_pattern(name):
    """Get chime pattern from custom or theme.

    Returns (chords, duration) tuple or (None, None) if not found.
    """
    if name in S.CUSTOM_CHIMES:
        custom = S.CUSTOM_CHIMES[name]
        return custom.get('pattern', []), custom.get('duration', 0.1)
    theme = CHIME_THEMES.get(S.CHIME_THEME, CHIME_THEMES['default'])
    if name in theme:
        return theme[name]
    return None, None
```

---

## 5. Settings Consolidation

### 5.1 Fix Inconsistent Defaults (BUG)

The `phrases` default is inconsistent across the codebase:
- Line 3634: `'hey computer, computer'`
- Line 3754: `'hey computer'`

**Fix immediately** - consolidate to single source of truth.

### 5.2 Nested Defaults Registry

```python
NESTED_DEFAULTS = {
    ('WAKEWORD_OPENWAKEWORD', 'model'): 'computer',
    ('WAKEWORD_OPENWAKEWORD', 'sensitivity'): 0.2,
    ('WAKEWORD_MACOS', 'phrases'): 'hey computer, computer',
    ('WAKEWORD_MACOS', 'cancel_phrases'): 'cancel, never mind',
    ('CHIME_AUDIO_SETTINGS', 'reverb'): 0.4,
    ('CHIME_AUDIO_SETTINGS', 'chorus'): 0.3,
}
```

---

## 6. Magic Numbers to Constants

```python
# Widget sizes
TRAFFIC_LIGHT_SIZE = 12
ICON_BUTTON_SIZE_SMALL = 20
ICON_BUTTON_SIZE = 24

# Timing (milliseconds)
SCROLL_DELAY_MS = 10
UI_UPDATE_DELAY_MS = 100
NOTE_HIGHLIGHT_DELAY_MS = 200

# Font sizes (pixels)
FONT_SIZE_SMALL = 10
FONT_SIZE_NORMAL = 11
FONT_SIZE_MEDIUM = 12

# Spacing
SPACING_SMALL = 4
SPACING_STANDARD = 8
SPACING_LARGE = 10

# Dialog margins
DIALOG_MARGIN = 15
DIALOG_MARGIN_LARGE = 20

# Border radius
BORDER_RADIUS_SMALL = 3
BORDER_RADIUS_MEDIUM = 4
```

---

## 7. Long-Term File Structure Recommendation

The file should eventually be split into:

```
voicething/
    voice_thing.py           # Main entry (~500 lines)
    settings.py              # Settings class + defaults
    chimes.py                # Chime themes and playback
    styles_helper.py         # CSS generation functions
    widgets/
        base.py              # DraggableDialog, TrafficLightButton
        waveform.py          # WaveformWidget
        knob.py              # RotaryKnob
        transcription.py     # TranscriptionRow, TranscriptionList
    dialogs/
        chime_editor.py      # ChimeEditorDialog + piano/grid widgets
        prefs.py             # PrefsDialog
        tmux.py              # TmuxSelectionDialog
```

---

## 8. Implementation Priority

### Phase 1: Quick Wins (Low Risk)
1. Remove dead imports and unused functions (~70 lines)
2. Fix inconsistent `phrases` default (bug fix)
3. Add `make_checkbox()`, `make_icon_label()` helpers

### Phase 2: Widget Helpers (Medium Risk)
4. Add remaining widget creation helpers
5. Add dialog helpers (`make_button_row`, etc.)
6. Add escape handling to DraggableDialog base

### Phase 3: Style Consolidation (Medium Risk)
7. Add CSS builder methods to BaseStyle
8. Consolidate vignette/shadow/grid drawing in BaseStyle

### Phase 4: Constants (Low Risk)
9. Extract magic numbers to named constants

### Phase 5: Audio Consolidation (Low Risk)
10. Add `get_effective_shift()`, `get_chime_pattern()` helpers

---

## Summary

| Category | Lines Saved | Risk Level |
|----------|-------------|------------|
| Dead code removal | 70 | Low |
| Widget helpers | 275 | Low |
| Dialog patterns | 135 | Medium |
| CSS/styling | 107 | Medium |
| Style file consolidation | 500+ | Medium |
| **Total** | **~1,100 lines** | |
